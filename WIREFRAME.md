# Wireframe — Route intent specs

각 라우트의 디자인 의도. 비주얼 검수자(`reviewer-visual`)가 "이 화면이 의도대로 만들어졌는가"를 판단할 때 참고.

---

## `/` — 대시보드 메인

**연결 이슈**: #3, #4

### 주 목적

탄소 담당자·경영자(비전문가 포함)가 화면을 열자마자 **이번 달 총 PCF 배출량과 전월 대비 변화**를 즉시 인지한다. 두 정보가 시각적으로 가장 우위에 있어야 한다.

### 정보 위계

1. **총 PCF 배출량** — 다른 KPI보다 시각적 강조(`bg-primary`). 가장 큰 숫자.
2. **전월 대비 변화율** — 같은 줄에서 두 번째. 증가 시 `text-destructive`(나쁜 신호), 감소 시 중립색.
3. **Scope 2 비중**, **Scope 3 비중** — 같은 위계로 좌우 또는 나란히. 비중 합 = 100%.

### 가장 중요한 액션

없음. 읽기 전용 화면. 활동 추가/편집은 `/activities`(별도 라우트)에서.

### 톤

모던, 깔끔, neutral. 강조는 첫 번째 카드 한 곳만. 색상 노이즈 최소화.

### 반응형

| 뷰포트           | 레이아웃           |
| ---------------- | ------------------ |
| 모바일 (375)     | 1열, 카드 4개 세로 |
| 태블릿 (768)     | 2×2                |
| 데스크탑 (1280+) | 4열 가로           |

가로 스크롤 절대 금지. 모든 KPI는 모바일에서도 잘림 없이 보임.

### 다크 모드

필수. 헤더 우측 토글 버튼으로 라이트↔다크. `next-themes` 사용. 모든 카드/텍스트가 다크에서도 가독.

### 데이터

- `useActivities()` + `useEmissionFactors()` (SWR)
- `calculateAllEmissions` → `aggregateByMonth` → 최신 월(`2025-08`) 기준
- 총 배출량: `formatEmission(latest.total)` → "2.23 tCO₂e"
- 전월 대비: `monthOverMonthChange(monthly, latest.month)` → "+12.4%" 형태
- Scope 2 = 전기 비율, Scope 3 = 원소재+운송 비율

### 의도적으로 이 PR에서 제외

- 실제 다크 모드 토글 동작은 `next-themes` 연결만 — 시스템 설정 자동 추적은 후속.
- 차트(트렌드, 도넛)는 다음 PR(#5, #6).
- 활동 추가 진입 버튼: `/activities` 화면이 생긴 후 헤더에 추가.
